pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "SampleSuperApp"
include ':app'

include ("customBiometric")
project(':customBiometric').projectDir = new File('../CommonSettings/customBiometric')

include ("LoginModuleApp")
project(':LoginModuleApp').projectDir = new File('../LoginModuleApp/app')